spec_version: 1
kind: blueprint
metadata:
  description: >
    Launch a single vanilla Ubuntu
inputs:
- AZURE_VM_SIZE: Basic_A1
clouds: 
  - azure: westeurope
  
applications:
    - ubuntu:
        input_values:
            - AZURE_VM_SIZE: $AZURE_VM_SIZE