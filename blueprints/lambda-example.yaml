spec_version: 1
kind: blueprint
metadata:
  description: Lambda Hello World example
    
clouds: 
  - AWS 2: us-west-2

inputs:
  - REGION: us-west-2
  - lambda_role_arn: arn:aws:iam::102305463663:role/colony/0fedd9bd-57d4-4537-aab2-e5a949e4d586/colony-power-user-role-0fedd9bd-57d4-4537-aab2-e5a949e4d586

applications:
  - lambda-app:
        input_values:
          - lambda_output: $colony.services.lambda-hello-world.outputs.lambda_output
        depends_on:
          - lambda-hello-world

services:
  - lambda-hello-world:
        input_values:
          - branch: $colony.repos.current.branch
          - sandbox_id: $colony.environment.id
          - REGION: $REGION
          - lambda_role_arn: $lambda_role_arn


debugging:
  availability: on
